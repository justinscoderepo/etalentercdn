{
  "name": "etalenter-web",
  "version": "1.0.0",
  "description": "eTalenter Web Application",
  "main": "index.html",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:preview": "wrangler deploy --env dev",
    "deploy:production": "wrangler deploy --env production",
    "pages:deploy": "wrangler pages deploy . --project-name etalenter-browser-worker",
    "pages:dev": "wrangler pages dev . --project-name etalenter-browser-worker",
    "build": "echo 'Static assets ready for deployment'",
    "pages:build": "echo 'Pages build completed - static assets ready'",
    "test": "echo \"No tests specified\" && exit 0"
  },
  "keywords": [
    "etalenter",
    "web",
    "application"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@cloudflare/puppeteer": "^1.0.4"
  },
  "engines": {
    "node": ">=14.0.0"
  },
  "directories": {
    "lib": "lib"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/justinscoderepo/etalentercdn.git"
  },
  "bugs": {
    "url": "https://github.com/justinscoderepo/etalentercdn/issues"
  },
  "homepage": "https://github.com/justinscoderepo/etalentercdn#readme"
}
